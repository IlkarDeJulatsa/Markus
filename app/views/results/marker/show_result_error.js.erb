# Marking state set to complete but there is at least one nil mark
jQuery('#criterion_incomplete_error').replaceWith("<%= escape_javascript(@result.errors[:base]).html_safe %>");
jQuery('#criterion_incomplete_error').show();
jQuery('#marking_state').val(<%= escape_javascript(Result::MARKING_STATES[:partial]).html_safe %>);
